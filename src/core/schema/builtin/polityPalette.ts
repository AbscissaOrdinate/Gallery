/**
 * Seed for `polityPalette` in `gallery.config.yaml`: the colours the political
 * map assigns to a polity whose record sets no `color`. Vault data, written
 * into the config once if absent (docs/STYLE.md §8); edit the config, not this.
 */
export const POLITY_PALETTE_SEED: readonly string[] = ["#c9663a", "#4f8fd6", "#6fbf95", "#d9a83a", "#b46fd1", "#e07a6a", "#5fc6c9", "#9aa0a6", "#8fbf3a", "#d16f9e"];
