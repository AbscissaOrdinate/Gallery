# Gallery

**The authoritative instructions are [`docs/CLAUDE.md`](docs/CLAUDE.md). Read it first.**

Then read [`docs/UNITS.md`](docs/UNITS.md) before writing any number — it is authoritative
over `gallery/05-designer-prompt` §8 and over `gallery/06` and `gallery/07`.

This file is a pointer so the repo root has one, and so the two do not drift apart.
